export const getters = {
    fixtures: state => {
        return state.fixtures
    },
    ticket(state) {
        return state.ticket;
    },
    balance(state) {
        return state.balance
    },
    tickets(state) {
        return state.tickets
    }
}
